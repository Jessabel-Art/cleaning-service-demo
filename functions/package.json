{
  "name": "sanchez-services-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "test": "mocha \"test/**/*.mocha.cjs\" --exit",
    "test:integration": "node integration/run-integration-test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.6.0",
    "pdf-lib": "^1.17.1",
    "stripe": "^20.0.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0"
  }
}
